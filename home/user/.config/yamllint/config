extends: default

rules:
  # デフォルトの行数制限は厳しすぎるので緩和
  line-length:
    max: 200
    level: warning
  # スタート記号を省略を許可
  document-start: disable
  # prettierと競合しない設定にする
  comments:
    min-spaces-from-content: 1
