# コマンドのガイドライン。

## 非推奨

- `cat`: 代わりにあなた自身がファイルを読み込んでください。
- `find`: 代わりに自前の検索か`fd`を使ってください。
- `gh api`: 代わりに`gh`の専用サブコマンドを使ってください。

## 禁止

- `git commit`: コミットは人間が行います。
- `rm`: 代わりに`trash`を使ってください。

- `grep`: 検索には`Search`や`rg`を使ってください。パイプのフィルタリングには使わずに出力内容を全部読んでください。

- `head`: 全部読んでください。
- `tail`: 全部読んでください。
